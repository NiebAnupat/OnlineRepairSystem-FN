import {Box, Container} from "@mantine/core";
import React from "react";

export const Role = () => {
    return (
        <>
            <Box bg={"gray.1"} h={"100%"} pt={'xl'}>
                <Container size={"90%"}>จัดการข้อมูลสิทธิ์ผู้ใช้</Container>
            </Box>
        </>
    );
};

export default Role;
