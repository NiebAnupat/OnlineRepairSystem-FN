import {Box, Container} from "@mantine/core";
import React from "react";

export const Worker = () => {
    return (
        <>
            <Box bg={"gray.1"} h={"100%"} pt={'xl'}>
                <Container size={"90%"}>จัดการข้อมูลช่างซ่อม</Container>
            </Box>
        </>
    );
};

export default Worker;
